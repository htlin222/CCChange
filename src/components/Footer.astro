---
import siteConfig from "@/site.config";

export interface Props {
  meta?: string;
  annotation?: string;
}

const {
  meta = 'A personal publication &middot; Travels &middot; Technology<br>And everyday notes',
  annotation,
} = Astro.props;
---

<footer class="border-border mt-12 border-t py-8 ">
  <div class="app-layout">
    <div class="flex flex-col items-center">
      <span class="block mb-3 text-lead font-semibold tracking-tight">
        {siteConfig.author}<span class="text-primary font-normal">.</span>
      </span>
      <span class="font-code text-muted-foreground text-balance tracking-wide text-ui text-center leading-relaxed" set:html={meta} />
      {annotation && (
        <span class="block text-muted-foreground max-w-80 mx-auto mt-6 text-ui text-center leading-relaxed">
          {annotation}
        </span>
      )}
    </div>
  </div>
</footer>
