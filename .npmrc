# lipi's plugins import transitive deps (unist-util-visit, @astrojs/markdown-remark…)
# that aren't declared in package.json. Use npm-style flat node_modules so pnpm
# resolves them the same way the upstream template expects.
node-linker=hoisted
